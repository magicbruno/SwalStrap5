<h1 id="sweet-alert-for-bootstrap-5">Sweet Alert for Bootstrap 5</h1>
<p>Un&#39;alternativa a <strong>Swetalert</strong> e <strong>Bootbox</strong>, basata sulle componenti Modal e Toast di Bootstrap 5. Cerchi una versione per Bootstrap 4? <a href="https://github.com/magicbruno/SwalStrap4">Go here</a>.</p>
<h2 id="per-iniziare">Per iniziare</h2>
<p>Carica Swalstrap da CDN:</p>
<pre><code>&lt;script src=&quot;https://cdn.jsdelivr.net/npm/@magicbruno/swalstrap5@1.0.8/dist/js/swalstrap5_all.min.js&quot;&gt;&lt;/script&gt;
</code></pre>
<p>swalstrap_all.js caricherà automaticamente il foglio di stile necessario e creerà un&#39;istanza predefinita della classe Swalstrap chiamata Swal (verranno creati anche gli alias: swal, Sweelelert e sweelelert).</p>
<p>Per utilizzare Swalstrap devi applicare il metodo fire() all&#39;istanza creata:</p>
<pre><code>&lt;script&gt;
    Swal.fire(&#39;Wanderful!&#39;,&#39;Swalstrap is working!&#39;,&#39;success&#39;)
&lt;/script&gt;
</code></pre>
<p>Se preferisci caricare separatamente il foglio di stile (o caricarne uno personalizzato):</p>
<pre><code>&lt;link rel=&quot;stylesheet&quot; href=&quot;https://cdn.jsdelivr.net/npm/@magicbruno/swalstrap5@1.0.8/dist/css/swalstrap.min.css&quot;&gt;
</code></pre>
<p>devi usare swalstrap.js:</p>
<pre><code>&lt;script src=&quot;https://cdn.jsdelivr.net/npm/@magicbruno/swalstrap5@1.0.8/dist/js/swalstrap5.min.js&quot;&gt;&lt;/script&gt;
</code></pre>
<p>In questo caso è a tuo carico creare almeno un&#39;istanza di swalstrap:</p>
<pre><code>&lt;script&gt;
    // Create an instance 
    const mySwal = new Swalstrap();
    // Then use it for all your popups
    mySwal.fire(&#39;Wanderful!&#39;,&#39;Swalstrap is working!&#39;,&#39;success&#39;);
&lt;/script&gt;
</code></pre>
<h2 id="download">Download</h2>
<p>In alternativa è possibile installare il pacchetto tramite npm:</p>
<pre><code>npm install @magicbruno/swalstrap5@1.0.8
</code></pre>
<p>clonare il repository Git:</p>
<pre><code>git clone https://github.com/magicbruno/SwalStrap5.git
</code></pre>
<p>or <a href="https://github.com/magicbruno/SwalStrap5/archive/refs/heads/main.zip">scaricarlo</a>.</p>
<blockquote>
<h3 id="avvertimento">Avvertimento</h3>
<p>Swalstrap è ispirato a Sweelelert, non è un clone della componente. Le caratteristiche sono riprodotte non copiate.
E quindi ci sono differenze. Controlla la documentazione e gli esempi.</p>
</blockquote>
<ul>
<li><a href="https://magicbruno.github.io/SwalStrap5/api.html">Documentazione</a>.</li>
<li><a href="https://magicbruno.github.io/SwalStrap5/basic-examples.html">Esempi</a>.</li>
<li><a href="https://magicbruno.github.io/SwalStrap5/custumization.html">Esempio di personalizzazione</a>.</li>
</ul>
